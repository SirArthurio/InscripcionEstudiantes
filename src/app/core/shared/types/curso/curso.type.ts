import { person } from '../person.type';

export type curso = {
  name: number;
  sede: string;
  institutionalEmail: string;
  personalEmail: string;
  academyProgram: string;
  competence: string;
};
