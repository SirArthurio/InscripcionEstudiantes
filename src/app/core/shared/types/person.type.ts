import { user } from './user.type';

export type person = {
  name: string;
  lastName: string;
  birthPlace: string;
  placeOfResidence: string;
  phone: string;
  documentNumber: string;
  documentType: string;
  gender: string;
};
