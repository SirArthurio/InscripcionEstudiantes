export type person = {
  id?: string;
  firstName: string;
  lastName: string;
  birthPlace: string;
  placeOfResidence: string;
  phone: string;
  documentNumber: string;
  documentType: string;
  gender: string;
};
