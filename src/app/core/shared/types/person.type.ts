import { user } from './user.type';

export type person = {
  name: string;
  lastname: string;
  birthPlace: string;
  placeOfResidence: string;
  phoneNumber: string;
  documentNumber: string;
  documentType: string;
  genre: string;
};
