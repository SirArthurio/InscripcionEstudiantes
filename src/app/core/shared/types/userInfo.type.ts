export type userInfo = {
  documentType: string;
  documentNumber: string;
  name: string;
  lastName: string;
  genre: string;
  birthDate: string;
  birthPlace: string;
  placeOfResidence: string;
  phoneNumber: boolean;
};
