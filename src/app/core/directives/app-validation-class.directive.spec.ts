import { AppValidationClassDirective } from './app-validation-class.directive';

describe('AppValidationClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AppValidationClassDirective();
    expect(directive).toBeTruthy();
  });
});
